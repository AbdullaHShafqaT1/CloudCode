# LegacyNode — NodeInsight Module Requirements
# Python 3.11+

# Async file I/O
aiofiles>=23.2.1

# Structured logging (matches LegacyBridge stack)
structlog>=24.2.0

# Optional: encoding detection (gracefully absent if not installed)
chardet>=5.2.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.7
